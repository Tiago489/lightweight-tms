// Initialize the app when the page loads
window.addEventListener("DOMContentLoaded", () => {
  console.log("lightweight-tms initialized");

  // Load a default component into #app
  loadPage("load-card");
});
